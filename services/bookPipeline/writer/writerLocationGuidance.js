/**
 * Shared instructions so verse carries the spread spec's world into read-aloud
 * (illustration-first books). Imported by draft + rewrite writers.
 */

const WRITER_LOCATION_VIVIDNESS_RULES = `
**Place on the page (every spread):** The listener must *see* where we are. Mine \`location\` and \`focalAction\` for concrete, paintable nouns or sensations (rope coils, lantern glass, tidepool shimmer, crowd hum, marble stairs, salt wind, bakery steam, festival banners). Do **not** flatten a rich \`location\` into only "inside," "room," "at home," or vague "there." If the spec names a pier, market, greenhouse, plaza, or trail, those ideas must appear in the verse in kid-simple words. Never contradict the spec or invent a new setting; **carry the spec's world into the language** so the art brief and the text match.
`.trim();

module.exports = { WRITER_LOCATION_VIVIDNESS_RULES };
